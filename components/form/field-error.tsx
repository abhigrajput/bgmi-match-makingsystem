/**
 * Kept at its original path so the forms written before the design system
 * keep importing it unchanged; the implementation now lives in components/ui.
 */
export { FieldError } from '@/components/ui/field-error';
